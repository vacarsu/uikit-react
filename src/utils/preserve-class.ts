export function presserveClass (preserve: string): string {
  switch(preserve) {
      default:
          return 'uk-preserve-color';
      case 'yes':
          return 'uk-preserve-color'
      
  }
}